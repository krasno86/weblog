%body{:style => "background-color: burlywood "}
  %br/
  %br/
  %br/
  %br/
  %br/
  %br/
  %br/
  %br/
  %br/
  %h2 Sign up
  = form_for(resource, as: resource_name, url: user_registration_path) do |f|
    = devise_error_messages!
    .field
      = f.label :email
      %br/
      = f.email_field :email, autofocus: true
    .field
      = f.label :password
      - if @minimum_password_length
        %em
          (#{@minimum_password_length} characters minimum)
      %br/
      = f.password_field :password, autocomplete: "off"
    .field
      = f.label :password_confirmation
      %br/
      = f.password_field :password_confirmation, autocomplete: "off"
    = flash[:recaptcha_error]
    = recaptcha_tags
    = button_tag "Sign up"

  -#%a.btn.btn-social-icon.btn-twitter
  -#  %span.fa.fa-twitter
  %br/
  -#%a.btn.btn-social-icon.btn-facebook
    %span.fa.fa-facebook

  /= link_to ('log in with facebook'), user_facebook_omniauth_authorize_path, :class => 'btn btn-facebook btn-lg'

  %br/

  %a.btn.btn-facebook.btn-lg{:href => "/users/auth/facebook", :title => "Facebook"}
    %i.fa.fa-facebook.fa-fw
    Log in with facebook

  %br/
  = render "devise/shared/links"
